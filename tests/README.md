# Synthetic tests

Run `python -m pytest tests -q` from the repository root. Tests cover geometry, calibration, fusion, gradients, checkpoint compatibility, losses, and metrics without training on datasets.
