# Model implementation

ReAxis modules and the AdaptCLIP-based backbone, adapters, tokenizer, and losses. The public import namespace is `reaxislib`. See [manuscript alignment](../docs/MANUSCRIPT_ALIGNMENT.md).
